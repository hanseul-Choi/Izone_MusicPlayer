<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:bind="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <variable
            name="viewModel"
            type="com.izone.musicplayer.viewmodel.MainViewModel" />

        <variable
            name="adapter"
            type="com.izone.musicplayer.recyclerview.MusicRepositoryAdapter" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".view.MainActivity">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/am_cl_progressBarLayout"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:elevation="1dp"
            android:clickable="true"
            android:background="@color/background_color"
            android:focusable="true">

            <ProgressBar
                android:id="@+id/am_pb_loading"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintRight_toRightOf="parent"/>

        </androidx.constraintlayout.widget.ConstraintLayout>

        <Spinner
            android:id="@+id/am_s_singerList"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="15dp"
            android:gravity="left"
            app:layout_constraintTop_toTopOf="parent"/>

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="#45808080"
            app:layout_constraintBottom_toBottomOf="@+id/am_s_singerList"/>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/am_rv_albumList"
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toBottomOf="@id/am_s_singerList"
            app:layout_constraintBottom_toTopOf="@+id/am_fl_miniplayer"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            bind:item="@{viewModel.musicList}"
            bind:listener="@{adapter.listener}"/>

        <FrameLayout
            android:id="@+id/am_fl_miniplayer"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            app:layout_constraintTop_toTopOf="@+id/am_v_setmini"
            app:layout_constraintBottom_toBottomOf="parent"/>

        <View
            android:id="@+id/am_v_setmini"
            android:layout_width="match_parent"
            android:layout_height="120dp"
            app:layout_constraintBottom_toBottomOf="parent"
            android:visibility="gone"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>